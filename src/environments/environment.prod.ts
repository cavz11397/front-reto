export const environment = {
    production: true,
    apiUrl: 'https://api.tu-dominio.com/items/', // URL de la API para producción
    // Otros valores de configuración específicos para producción
  };