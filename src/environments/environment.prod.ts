export const environment = {
    production: true,
    apiUrl: 'https://back-reto.onrender.com/', // URL de la API para producción
    // Otros valores de configuración específicos para producción
  };